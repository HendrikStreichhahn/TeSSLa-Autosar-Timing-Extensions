# run with traces 3 of delayConstraint

include "../main/t2uConstraints.tessla"

in source: Events[Unit]
in target: Events[Unit]

def lower = 5
def upper = 5

out strongDelayConstraint(source, target, lower, upper)
