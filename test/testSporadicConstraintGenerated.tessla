include "../main/t2uConstraints.tessla"

in x: Events[Unit]
def lower = 100
def upper = 150
def jitter = 10
def minDist = 10

def constraint = sporadicConstraint(x, lower, upper, jitter, minDist)

out filter(constraint, !constraint)
