include "../main/t2uConstraints.tessla"

in source: Events[Int]
in target: Events[Int]

def lower = 4
def upper = 10

def constraint := delayConstraint(source, target, lower, upper)

out filter(constraint, !constraint)
