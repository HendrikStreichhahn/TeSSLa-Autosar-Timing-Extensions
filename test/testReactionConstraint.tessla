include "../main/t2uConstraints.tessla"

in stimulus: Events[Int]
in response: Events[Int]

def minTimeDiff : Int = 10
def maxTimeDiff : Int = 30

out TADL2.reactionConstraint(stimulus, response, minTimeDiff, maxTimeDiff)
