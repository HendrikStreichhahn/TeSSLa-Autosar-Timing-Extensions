include "../main/t2uConstraints.tessla"

in stimulus: Events[Int]
in response: Events[Int]

def minTimeDiff : Int = 0
def maxTimeDiff : Int = 0


out TADL2.ageConstraint(stimulus, response, minTimeDiff, maxTimeDiff)
