include "../main/t2uConstraints.tessla"

in start: Events[Unit]
in preempt: Events[Unit]
in resume: Events[Unit]
in end: Events[Unit]

def lower = 50
def upper = 75

def constraint = executionTimeConstraint(start, end, preempt, resume, lower, upper)

out filter(constraint, !constraint)
