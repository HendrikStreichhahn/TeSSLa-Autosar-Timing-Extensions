include "../../main/t2uConstraints.tessla"

in event: Events[Int]

def minVals = List_append(List_append(List_append(List_empty[Int], 10), 20), 30)
def maxVals = List_append(List_append(List_append(List_empty[Int], 15), 25), 35)

def constraint = arbitraryConstraint(event, minVals, maxVals)

out constraint
