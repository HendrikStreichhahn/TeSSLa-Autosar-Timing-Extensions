include "../../main/t2uConstraints.tessla"

in event: Events[Int]

def periodX : Int := 50
def offset: Map[Int, Int] := Map_add(Map_add(Map_add(Map_empty[Int, Int], 0, 10), 1, 20), 2, 30)
def jitter: Int = 5
def mini: Int = 1

def constraint = patternConstraint(event, periodX, offset, jitter, mini)

out constraint