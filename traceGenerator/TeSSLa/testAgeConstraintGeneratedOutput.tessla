include "../../main/t2uConstraints.tessla"

in stimulus: Events[Int]
in response: Events[Int]

def minTimeDiff : Int = 5
def maxTimeDiff : Int = 20

def constraint = ageConstraint(stimulus, response, minTimeDiff, maxTimeDiff)

#out on(mergeUnit(stimulus, response), constraint)
out constraint
